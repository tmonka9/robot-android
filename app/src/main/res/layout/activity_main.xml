<?xml version="1.0" encoding="utf-8"?>
<!-- Home page content (inside the shell). -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/screen_padding">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="28dp"
            android:gravity="end|center_vertical"
            android:orientation="horizontal">

            <include layout="@layout/view_system_status" />
        </LinearLayout>

        <!-- Hero and card grid: side by side on tablets, stacked on phones (BaseActivity#setupColumns). -->
        <LinearLayout
            android:id="@+id/columns"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp">

            <FrameLayout
                android:id="@+id/hero"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_hero"
                android:minHeight="@dimen/hero_min_height"
                android:tag="1.1">

                <ImageView
                    android:layout_width="@dimen/hero_robot"
                    android:layout_height="@dimen/hero_robot"
                    android:layout_gravity="end|bottom"
                    android:contentDescription="@null"
                    android:scaleType="fitEnd"
                    android:src="@drawable/img_robot" />

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="start|center_vertical"
                    android:orientation="vertical"
                    android:paddingStart="28dp"
                    android:paddingEnd="16dp">

                    <TextView
                        android:id="@+id/hero_ai"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:fontFamily="sans-serif-black"
                        android:includeFontPadding="false"
                        android:text="@string/title_ai"
                        android:textColor="@color/blue"
                        android:textSize="@dimen/hero_ai" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="4dp"
                        android:fontFamily="sans-serif-medium"
                        android:letterSpacing="0.08"
                        android:text="@string/title_rest"
                        android:textColor="@color/text_primary"
                        android:textSize="@dimen/hero_title" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="10dp"
                        android:text="@string/hero_tagline"
                        android:textColor="@color/blue_light"
                        android:textSize="@dimen/hero_tagline" />
                </LinearLayout>
            </FrameLayout>

            <!-- Filled by MainActivity#buildCardGrid -->
            <LinearLayout
                android:id="@+id/home_grid"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:tag="1" />
        </LinearLayout>
    </LinearLayout>
</ScrollView>
